[project]
name = "beyond-the-basic-stuff"
version = "0.1.0"
description = ""
requires-python = ">=3.13"

[tool.ruff]
lint.select = [
    # Pyflakes
    "F",
    # Pycodestyle
    "E",
    "W",
    # isort
    "I",
    # flake8-bandit
    "S",
    # flake8-bugbear
    "B",
    # Pylint
    "PL"
]
lint.ignore = [
    # tests use assert
    "S101"
]

[dependency-groups]
dev = [
    "ruff>=0.14.8",
]
